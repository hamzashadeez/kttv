import { Text, View } from "react-native";

const Leave = () => {
  return (
    <View style={{ backgroundColor: "seagreen", flex: 1 }}>
      <Text>Leave</Text>
    </View>
  );
};

export default Leave;
